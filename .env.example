# 内置 LLM（默认关闭；核心使用方式是外部 Agent 调用本工程）：false | true | mock
ENABLE_BUILTIN_LLM=false

# 仅 ENABLE_BUILTIN_LLM=true 时需要（MiniMax 等 OpenAI 兼容）
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.minimaxi.com/v1
OPENAI_MODEL=abab6.5s-chat
